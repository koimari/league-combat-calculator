# Surface-area backlog

What the surface-area, coverage-frontier (rows `CF*`), engine-retirement (rows `ER*`)
and closeout (rows `SC*`) campaigns surfaced and did not close. Delete a row when its
fix lands; this file is the one home for the list. Traps live in `CLAUDE.md` Known Quirks.

| # | Where | What | Action |
|---|---|---|---|
| E14 | `scripts/load_sanity.py` | Updated in 39d3794 for `checks.cache`; not run live (needs `DATABASE_URL`/`REDIS_URL`). | Run once against a deployed target. |
| E12 | `tests/coverage_resolver.py` + `test_coverage_claims.py` + `coverage_evidence.py` | Measured (closeout): resolver tax is 219 ms of an 8.3 s collection, ≈0.4% of a full `-n auto` run; `test_coverage_claims.py` 11.3 s paid once per full run. | No win available; re-measure only if the full run's shape changes. |
| CF11 | `data/champions.json` | Cache quality: Milio P 40 values/18 levels; Miss Fortune P two rows, no level axis; Dr. Mundo/Rek'Sai/Zac P + Tahm Kench E heal rows labelled `Max Health Damage`; Jayce P duplicate entries; Nimbus Cloak keyed by summoner cooldown; Jack of All Trades drops 8/12/20; Singed W has no 3 s root/ground atom (only the 0.375 landing delay); Twitch W resolves zero rows. | Parser fixes on patch day, each pinned; until then the modules carry `HARDCODED` constants and Singed W / Twitch W stay magnitude-less. |
| CF17 | `champions/rumble.py` P | Overheated stays unpriced: the blocker is the unsourced start time (Overheat opens at 100 Heat and no heat is simulated), not a lockout primitive — ruled and pinned in the module. | A heat axis, then price both halves. |
| ER2 | `champions/{sivir,vex,aphelios,annie,aatrox}.py`, `data/practice-corpus/` | Still coarse with cached evidence for each: Sivir Q (no cadence) / W (8 bounces aggregated), Vex R (one part sums two spaced hits), Jayce R cannon (wants `empowers_next_auto`), Aphelios P/W (zero rows price no weapon branch), Annie E (no landing row). Annie P / Kennen P are stack walks (cross-slot charge; per-target consume), not a first-cast marker. Aatrox Q/W kinds are sourced but re-timing breaks the pinned corpus + `oracle-P4B-leaf29.json` (one heal at t=0 vs 3/2 events). | Stack walks for Annie/Kennen; re-pin corpus and oracle with the Aatrox re-timing; the rest need cache or a new empower primitive. |
| ER7 | `tests/test_phase0_sentinels.py`, `tests/test_program_caches.py` | Count pins and AST tests ruled intact: non-vacuity guards and the cache-key-covers-reads lint. | Revisit only if one goes red on a pure refactor. |
| SD9 | `item_effects.py` ladder vs `item_behavior_catalog` | `on_hit_heal` retired with parity proven; CRIT_PROFILE and DAMAGE_ROUTING need a fight-free flat reader (mirror `SUSTAIN_PAYLOAD_REFERENCES`) before their tags can retire; `magic_damage_amp` has no catalog compiler. `tests/test_tag_dispatch_parity.py` gates all of it. | One flat reader per family, then retire tag by tag. |
| SC1 | `static/js/app.js` Jayce Q | Jayce's Q binds `accelerated_q` and sits outside the `hammer_stance` mirror group, so a payload can carry `hammer_stance: true` and `accelerated_q: true` — same two-axis family C10 fixed, different slot-binding cause. | Bind the Q variants to the form axis. |
| SC2 | `static/js/app.js` `optimize` controls | The `optimize` control family can never be gated by `applyControlCapabilities`: its controls are created by a later render, after the boot-time sweep. | Gate at render time or state the exemption. |
| SC3 | `champions/sivir.py` W | The pinned packet AD ratio `[0.20…0.40]` matches the cached **Bonus Attack Speed** row, not **Bounce Damage** `[40…50]% AD` — prices 40.8 where the damage row gives 51.0. | Re-point the packet at the damage row; golden re-capture. |
| SC4 | `champions/aphelios.py` | The `applies_item_on_hits` block sits on the `calibrum` branch but Duskwave is `infernum`; W stores the 0.25 s swap duration in `damage_entry`'s cooldown slot (cached cooldown 0.8). | Fix both when Aphelios' weapon branches land (ER2). |
| SC5 | `champions/{katarina,kennen}.py` | `"event_order_certified": True` (bool) is dead — `damage.py` requires a str — and both rows are in the golden, so removal moves it. | Drop with a golden re-capture. |
| SC6 | `champions/{xin_zhao,riven,master_yi}.py` | Ready consumers of the new crit vocabulary: Xin Zhao Q's sourced crit sentence wants part-level `crit_effectiveness=1.0`; Riven P and Master Yi P carry "priced non-crit, conservative" notes with sourced crit sentences. Xin Zhao W's thrust `0%:33.3% (based on crit chance)` is a different, degraded axis. | Declare the three; the W axis needs its own key. |
| SC7 | `champions/shyvana.py` | `scalemail_stacks` option is inert: its parser returns `None` before reading it. | Wire or cut the option. |
| SC8 | `survival/` published redirect | A gate-cancelled redirect's published parent event still shows the 86% direct share while the walk applies the restored 100% — the receipt list holds the original, the walk mutates a copy. Numbers right, one published field understates. | Publish from the walk's copy. |
| SC9 | `scenario.ChampionLoadout.resolve`, `golden_snapshot._coupled_receipt` | The "same" pre-combat stat surface has three recipes: `resolve` omits `external_stat_bonuses`, the coupled receipt omits `rune_page`, the API composes both. | One composition helper all three call. |
| SC10 | `champions/ornn.py` W | `% of target's maximum health` in the cache but no `target_max_health_sensitive` stamp, unlike ~30 other modules (R has it). | Stamp it. |
