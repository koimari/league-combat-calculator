# Surface-area backlog

What the surface-area, coverage-frontier (rows `CF*`), engine-retirement (rows `ER*`),
closeout (rows `SC*`) and resolution (rows `SR*`) campaigns surfaced and did not close.
Delete a row when its fix lands; this file is the one home for the list. Traps live in
`CLAUDE.md` Known Quirks.

| # | Where | What | Action |
|---|---|---|---|
| CF11 | `data/champions.json` | Cache quality: Milio P 40 values/18 levels; Miss Fortune P two rows, no level axis; Dr. Mundo/Rek'Sai/Zac P + Tahm Kench E heal rows labelled `Max Health Damage`; Jayce P duplicate entries; Nimbus Cloak keyed by summoner cooldown; Jack of All Trades drops 8/12/20; Singed W has no 3 s root/ground atom (only the 0.375 landing delay); Twitch W resolves zero rows. Resolution-campaign additions, each pinned in its module: Sivir P is flat per the cache with a rankless five-value row and no uptime; Sivir Q has no travel/return timing atom (`speed` is two unlabelled values); Singed P's only number is the ambiguous atom `MSPercent = 0.25` (per-stack vs total, 25× span); Aphelios Calibrum/Infernum/Crescendum numbers are prose over empty `leveling` rows (the first two priced from the stated sentences, Crescendum unpriced). | Parser fixes on patch day, each pinned; until then the modules carry `HARDCODED` constants or stay magnitude-less with the missing row named. Audited 2026-09-15: every champion named above carries the note in its own module, Jayce's calling the duplicate entries a parser artifact and reading entry [0], Twitch emitting a zero-damage row, Sivir naming the rankless row and the absent uptime. The mitigations are in place, so what is parked here is the PARSER work, which needs a fresh pull to measure against and belongs to patch day rather than to this campaign. |
| ER5 | `src/calculator/` tail | The rule-5 lint scans the whole package; `tests/test_literal_defaults.py` partitions it into `ROOTS` (98 roots, 421 files) and `ER5_TAIL` (89 modules, 1,264 sites). NO tail site is unexamined. `scripts/tail_site_triage.py` classifies every one and `docs/receipts/er5-tail-triage.json` records the split: 704 (56%) read keys that are not row fields at all; 391 (31%) sit in modules whose suites pin them TOLERATING malformed input, where indexing turns graceful degradation into a crash or a silent drop; 147 (12%) read a censused key against something that is not a row; 22 are ADJUDICATED, examined against a measured corpus and kept with the measurement recorded. Two gated corpora back it, `tests/test_row_stream_census.py` over nine published streams and `docs/receipts/internal-row-census.json` over the engine's internal rows across all 173 champions, and the five-clause rule lives with the first. Three producers have named contracts: `cast_event_row`, `damage_event_row`, `heal_event_row`. | Nothing outstanding. A NEW tail site lands as `CANDIDATE` and a test proves the classifier still produces that class, so zero means finished rather than broken. |
| SR1 | `roster_composition.actor_params` | CLOSED. A roster actor wore the attacker's minor runes and stat shards, so an enemy Aatrox earned Scorch damage and Adaptive Force it never selected (about 23.6 damage over ten seconds for Scorch alone, before the shards' stat grant). This was not an open product question: the keystone was ALREADY blanked for a non-main actor, and the other three fields were an incomplete version of that same decision. All four are blanked together now, plus `rune_options`, which was inherited too. Giving a roster actor its OWN page stays a feature and not this field's answer. `tests/test_roster_composition.py` pins all four for an ally and an enemy, and pins that the attacker keeps the page it selected. Neither golden moved: no golden scenario puts a rune page on an attacker that also has a roster. | Nothing. An ally rune page of its own is a feature to spec, not a defect to fix. |
| SR2 | `static/js/app.js` `optimize` family | The optimizer has NO user-facing entry point at all, which is larger than this row used to say. `/api/optimize` is called from exactly two places in `app.js`, inside `startRosterOptimization` and `startOptimizeBuild`; those two are called from exactly three places, all in the click delegate, all keyed on `data-optimize-roster`, `-roster-all` and `-build`; and those attributes are emitted by no renderer and no template. Git history shows they were never mounted in any commit, so this is a feature whose UI was never built rather than one whose buttons were removed. The backend is not abandoned: `/api/optimize` passes the 10-scenario acceptance matrix and a 173-champion optimizer matrix on every CI run, and the capability contract publishes the locator. The acceptance matrix's `optimize_submitted: True` is a hardcoded literal about the API call and is NOT evidence of a mounted control. | Product call, and the two options are not symmetric. Mounting finishes a feature whose API, gates and contract are already maintained; deleting the handlers and the locator removes the only client code for a backend the CI exercises 183 times a run. Recommend mounting. Not implemented: this needs a decision, not a guess. |
| SR3 | `item_effects.py` + `ally_effects` registries | CLOSED. Abyssal Mask's Unmake share is one number in the source and is one number here: `ABYSSAL_UNMAKE_MAGIC_AMP`, declared once and read by both registries, `_REFERENCE_ITEM_EFFECTS` as `magic_amp` for the holder's own magic and `ALLY_ITEM_EFFECTS` as `magic_damage_amp` for an ally benefiting from the same curse. Two spellings for two subjects is the real shape, so the spellings stay; only the duplicated literal goes. `tests/test_item_effects.py` reads both registries against the constant and also asserts the source writes it once, because two equal literals would pass the read. | Nothing. |
| SR4 | `champions/charge_cadence.py` stocks | A charge slot prices its cached `rechargeRate`, the shared scheduler spends the cached stock, and all 21 charge slots carry a reviewed `ChargeRule`. Nine stocks stay at one banked cast. Audited 2026-09-15: the row's premise, that no cached field states the field cap, is TRUE for seven of the nine and false for two. Heimerdinger Q states 'Up to 3 H-28G Evolution Turrets can be deployed at a time' and Bard W states 'Up to 3 shrines may be active at a time'; both modules asserted the opposite in their `ChargeRule` and both now say what the cache says. Heimerdinger needs no further change: the `q_turrets` option already prices exactly the cached cap of 3, which is WHY the slot banks one deploy rather than three. Bard is CLOSED: its shrine's heal is priced at the cast, so the cast is the packet and the persistent-object exemption does not reach it, and the slot now banks its sourced stock of 2. Measured at twenty seconds, shrines land at 0.25s and 0.5s then 18.5s where before there were two in the window; exactly one golden leaf moved, `W/charge_pool` from absent to 2. Caitlyn W still has no cached stock at all. | Source the field cap for the seven that genuinely state none, and the Caitlyn W stock. Each is a cache question, not a modelling one. |
| SR5 | `champions/engine.py` empower keys | CLOSED. `empowers_next_auto` sub-keys are key-validated the way entry, `target_debuff` and `post_hit_proc` keys already were, against `entry_shape._ALLOWED_EMPOWER_KEYS`: `hits`, `attack_speed`, `authored_timing`, `rides_scheduled_auto`, `cooldown_starts_after_hits`, `swing_parts`. A misspelling raises at parse time naming the champion, the slot and the key, instead of the reader's `.get` answering `None` and the empower quietly not doing what the module asked. A second test scans `src/calculator/fight/` for every reader and fails if one reads a sub-key the vocabulary does not allow, which is what caught `swing_parts`: a first cut of the vocabulary missed it and took every Camille parse down. | Nothing. |
| SR6 | `campaign-fingerprints.json` `coupled_golden_exact_values` | CLOSED by retiring the digest and replacing it with a gated one. The committed sha reproduced under no reading of its own stated rule on any tree it was checked against: the current capture, `origin/main` and two earlier trees, four encodings each. Nothing read the field, so nothing had ever failed on it, and a drifted number and a mistyped one were indistinguishable. The rule is code now, `golden_snapshot.exact_values_digest`, and three tests hold it: one recomputes the committed digest, one asserts the retired sha is marked and cannot be mistaken for current, and one moves a total and asserts the digest moves with it. That negative caught its own first draft, which set a total to `0.0` when the first total in the capture is already `0.0` and so moved nothing. | Nothing. |
| SR7 | champion options | CLOSED for proc counts: `docs/coverage-status.md` reads 0 in-fight counts still asked, and every one of the 354 champion options carries a reviewed reading. Two are blocked on a number no source here states (Heimerdinger's turret and Kindred's Wolf attack rates) and the page names each. | Source those two rates, or leave them asked. |
| SR8 | cast pricing | CLOSED. `docs/coverage-status.md` reads 0 in-fight stack levels, beside the 0 in-fight proc counts SR7 closed. Every shape has a home and a champion on it. The CONSUMING shape is Case 6's `stack_window` (Tristana, Yasuo, Varus, Mel, Kalista, and now Hecarim's damage), with a rule for which side of the cast its window sits on. The RAMP shape is `swing_ramp` where the grant is attack speed, walked by `rearmed_swings.swing_times` beside a build's item ramp, and `stat_ramp` where it is anything else, served by `champions/stat_ramp.py` as the level's time-weighted mean. A count that gates a rider is `armed_procs`, with `retained_at_threshold` for a state the swing reads rather than a charge it spends, and `arming_slots` for a counter only a marked target feeds. A count that gates a CAST is `cast_requires_stacks`. A count that gates a HEAL is `heal_requires_stacks`. A count that shortens its own slot's cooldown is `stack_scaled_cooldown`, walked FORWARD so the level a cast leaves behind decides when the next one lands. Two options stay blocked on a number no source here states (Heimerdinger's turret and Kindred's Wolf attack rates) and `docs/coverage-status.md` names each. The `t=0` seed is settled: every ramp opens EMPTY, because the attack that grants a stack cannot be rated by it, and two tests in `tests/test_interp_charged_strike.py` had said so in their docstrings while asserting the opposite. | Source the two missing attack rates, or leave them asked. |
| SR9 | `static/js/app.js` champion options | CLOSED. An option the engine DERIVES now declares `derives=True` on its row, 30 of them across 29 modules, and the page leaves its key ABSENT until the user states a level: `seededChampionOptions` skips a derived key on reset and `engineChampionOptions` omits one that holds no value, so the request carries what the engine reads as "derive this". The stepper gained an `Auto` state to hand the option back, and stepping off Auto lands on the declared default rather than one step past it. `tests/test_derived_champion_options.py` scans the modules for the `is None` branch and fails a derived option that forgets the flag, with the two non-derive `is None` uses named and excused (`fight_duration_seconds` is engine-injected, Aphelios saves and restores `q_variant` around a per-weapon override). Measured through the Flask route: Hecarim Q prices 486.9 with the key omitted, 276.5 at a stated zero and 605.1 at a stated three. | Nothing. |
