# Surface-area backlog

Everything the coverage-frontier (rows `CF*`), engine-retirement (`ER*`), resolution
(`SR*`), rotation-derivation (`RV*`), source-review (`SA*`) and code-health (`CH*`) work
surfaced and did not close. Delete a row when its fix lands; this file is the one home for
the list. Traps live in `TRAPS.md`.

| # | Where | What | Action |
|---|---|---|---|
| CF11 | `data/champions.json` | Cache quality. Milio P 40 values over 18 levels; Miss Fortune P two rows with no level axis; Dr. Mundo, Rek'Sai, Zac P and Tahm Kench E heal rows labelled `Max Health Damage`; Jayce P duplicate entries; Nimbus Cloak keyed by summoner cooldown; Jack of All Trades drops 8/12/20; Singed W carries no 3 s root atom, only the 0.375 landing delay; Twitch W resolves zero rows; Sivir P is flat with a rankless five-value row and no uptime; Sivir Q has no travel or return timing atom; Singed P's only number is the ambiguous `MSPercent = 0.25`; Aphelios Calibrum, Infernum and Crescendum are prose over empty `leveling` rows. | Parser fixes, on patch day, against a fresh pull. Every champion named carries the note in its own module and prices the mitigation, so what is parked here is the parser work only. |
| SR4 | `champions/charge_cadence.py` stocks | Nine charge slots bank one cast because a field cap binds them, and no source states that cap: no prose sentence, and the spell objects carry cast and missile geometry with no cap among their scalar fields. The tracked binaries do carry `mMaxAmmo` per rank (Caitlyn W 2/3/3/4/4/5/5, Gangplank E 3/3/3/4/4/5/5, Jhin E, Zyra W and Azir W 2, Ivern W and Heimerdinger Q 3), pinned by `tests/test_blocked_on_absent_data.py`. | Source the field cap. Reading `mMaxAmmo` into `charge_cadence` has no consumer on its own, because every rule banking one cast does so for a reason that is not a missing stock. |
| SR7 | champion options | Two options ask for a count the fight could walk if one number were sourced: Heimerdinger `q_turret_attacks` needs the turret's attack speed, and Kindred `w_attacks` needs Wolf's base attack rate. `docs/coverage-status.md` names both. | Source the two rates, or leave them asked. |
| RV1 | `rotation_resolver` | The derivation sees no edge for a setup the atoms cannot express, so the champion falls back to base cast order. Three shapes: a self-edge, where one slot applies and consumes its own mark (Tristana E, Caitlyn W, Volibear W, Naafiri Q, Yasuo Q3); an auto-stream edge, where the consumer is a basic attack rather than a cast slot (Viego Q, Kennen P, Nidalee W); and a prose-only relationship (Illaoi E's vessel transfer, Gnar's form swap, Renekton and Rumble's self-resource amp). Hwei and Seraphine derive an execute-last order that needs a timed-fight reading to confirm. Seven more modules carry a mechanic with no ordering atom at all: Nautilus, Lulu, Xin Zhao, Malphite, Poppy, Kai'Sa plasma autos and Varus's R-stack seed exception. | Author the missing ordering atom per shape, or declare a module `CAST_ORDER`. `docs/rotation-design.md` owns the scoring model. |
| ER1 | `interpreters._LEDGER_CONTRIBUTION_NOTES` | Eclipse's survival-ledger note is derived from the declaration, keyed by `SurvivalLedgerContribution.SELF_SHIELD`, so it states the modelled behaviour and nothing else. The hand-written note it replaced also disclosed that the strongest-shield rule is not modelled, and a derived note has no field to carry that. The modelled behaviour, additive stacking, is pinned by `tests/test_eclipse_shield_selection.py`. | Give the contribution a declared disclosure field, so the caveat rides the derived note instead of returning as per-item prose. |
| SA1 | `champions/gwen.py` | A Thousand Cuts heals 50% of post-mitigation champion damage in the module (`derive_self_healing`), where the cached description says 67%. The per-instance cap is read from the cache and is correct. | Re-read the share from the cache and re-capture the golden, which moves every Gwen self-healing leaf. |
| SA2 | `champions/camille.py` | Adaptive Defenses prices `ADAPTIVE_DEFENSES_MAX_HP_RATIO = 0.20` flat, where the cached passive gives 10% / 15% / 20% based on level, so the shield is overstated at every level below the top band. The ratio is prose-only in the cache, with no amount row in the dump. | Read the level axis out of the passive prose and re-capture the golden, which moves every Camille shield leaf under level 18. |
| SA3 | `champions/yasuo.py` | The Flow row's `detail` calls the shield a "magic-damage shield". The cached passive says Yasuo "consumes all Flow to grant himself a shield" and names no damage type. The amount is right, the published sentence is not. | Drop the damage type from the sentence and re-capture the coupled golden, which carries the detail string. |
| SA4 | `stats.py` adaptive basis | The adaptive-force choice compares totals that include item passives `Adaptive_force` says do not count: Staff of Flowing Water's Rapids, Riftmaker's Void Infusion and the Awe items, all inside `StatBonuses.bonus_ap` and `bonus_ad`. | Build the basis from the counting passives only, and re-capture the golden where a build's choice flips. |
| CH1 | `champions/module_helpers.py` | All 27 cross-slot `ctx.ranked("<slot>")` calls are guard sites wanting a `ranked_at(slot, index)` factory, and `module_helpers.py` has no room for one under its 500-line cap. | Add the factory in a sibling module and convert the guards. |
| CH2 | champion prose | 8 prose sites in 5 files name two option keys neither `Rumble` nor `Seraphine` declares. | Repoint each site at a declared key or drop it. |
| CH3 | `champions/shared_option_keys.py` | `w_charge` is declared by Irelia and K'Sante both, so the K'Sante Path Maker walk publishes a `w` resource ledger on an Irelia fight that sets it. Every `fight/stacks/` gate has that shape. | Gate each stack walk on the champion as well as the option key. |
| CH4 | `scripts/literal_defaults.py` | A named option key is not resolved, so a `.get(OPTION_KEY, literal)` site is invisible to the report. | Resolve module-level string constants before matching. |
| CH5 | `item_effects.sustain_effect_value` | Read 134 times over 20 files, mostly for values that are not sustain. | Rename it in a unit of its own, since `item_effects.py` is rule 5's home. |
| CH6 | `item_support_everlasting._everlasting_packets` | 264 lines, the largest block left after the item-support carve. | Carve it by producer. |
| CH7 | `tests/` | `scripts/one_spelling.py` reads only `src/` and `scripts/`, and `tests/` holds 84 findings over 38 files, three of them the gate's own fixture. | Codemod the findings, then extend the gate to `tests/`. |
| CH8 | `scripts/assignments/ledger_projection.json`, `docs/receipts/expected-golden-diff-slots18-alistar-r.json` | Each cites a class that no longer exists: `UndeclaredStatRead` and `DuplicateSumMember`. | Drop the citations. |
| CH9 | `scripts/tail_site_triage._tolerance_modules` | `from src.calculator.<pkg> import <module>` credits the package, not the module, so a tolerance pin written that way credits nothing. | Resolve the imported name as a submodule first. |
| CH10 | `scripts/behavior_frontier.ZERO_POLICY_ISSUE` | The tree's last `issue #N` citation, published as a field of `docs/behavior-frontier.json`. | Drop it, `--write`, and refresh the fingerprints. |
| CH11 | `behavior_frontier._receiver`, `tail_site_triage._receiver` | One receiver rule in two scripts. The `tail_site_triage` copy compares a whole dotted path against the bare names in `NON_ROW_RECEIVERS`, so six cached-champion reads and two stat-block reads in `participant_timeline.py` carry an engine-row bucket. | One shared receiver that compares the root name. |
| CH12 | `timeline/utility.py` | 42 literal-default sites that cannot fall until the support and healing books are censused the way `internal_row_census.py` censuses the damage books. | Census those books. |
| CH13 | sightline | #55 owns arity but runs in no CI job, and #23 complexity has no owner that fails, so a new wide signature or complex function fails nothing. | Run `sightline gate` in CI against `.sightline-baseline`. |
| CH14 | `scripts/receipt_walk_schedule.py` | The label `survival.actions.SurvivalAction.<field>` names the home the action split retired. | Relabel, `--write`, and refresh the fingerprints the golden reads. |
| CH15 | `calculate.py` | Two `.get("timeline_coverage", {})` calls hand an empty dict to a reader that indexes. | Index directly, so a missing key raises. |
| CH16 | `item_effects._REFERENCE_ITEM_EFFECTS` | Four `CodeOwned` values are also emitted by the parser, so the literal wins silently. Thornmail, Fimbulwinter and Unending Despair agree with the parse. Essence Reaver's `mana_restore_crit_ratio` is 25.0 in code and 0.25 in the parse, whose `_parse_essence_reaver_spellblade` hard-codes `1.25 * 0.5`. | Make the three parser-owned, source the Essence Reaver pair, then fail the parity test on any parsed key that is `CodeOwned`. |
| CH17 | `survival/transitions.py`, `fight/after/lethality_windows.py`, `participant_timeline.py`, `program/compile.py` | Five inline copies rescale a mitigated amount between two resistances, with different refusal checks. The support-shred copy clamps a negative baseline to 0 after its percent cut, which `resistance.reduce_resistance` never does, so the shred lowers damage on a target below 0. | One `resistance.rescale_mitigated` the five call. |
| CH18 | deferred imports | Twelve function-local imports guard no cycle: six in `fight/stacks/`, `champion_opening_defenses`, `support_bailout`, three in `item_support_actives` and one in `ally_packet_shape`. The first eight sit in modules whose top-level imports already load the `champions` package. `cleanse_declarations` and `item_effects`, the targets of the other four, never import their importer back. | Hoist them. |
