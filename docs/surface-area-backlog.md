# Surface-area backlog

What the surface-area campaign (`docs/plans/2026-08-20-surface-area-campaign.md`) and the
coverage-frontier campaign (`docs/plans/2026-08-20-coverage-frontier-campaign.md`, rows `CF*`)
surfaced and did not close. Delete a row when its fix lands; this file is the one
home for the list. Traps live in `CLAUDE.md` Known Quirks.

| # | Where | What | Action |
|---|---|---|---|
| D3 | ~40 custom parsers (`akshan.py:257`, `darius.py:215`, …) | `entry["event_order_certified"] = "single_hit"` hand-assigned — the fact, not a wrapper; fine, but `single_hit_slots` now exists for packet rows. | Opportunistic. |
| E14 | `scripts/load_sanity.py` | Updated in 39d3794 for `checks.cache`; not run live (needs `DATABASE_URL`/`REDIS_URL`). | Run once against a deployed target. |
| E12 | `tests/coverage_resolver.py` (1.8k) + `test_coverage_claims.py` (2.7k, subprocess pytest at `:654`) + `coverage_evidence.py` (1.1k) | 5.6k lines proving evidence strings name real symbols, run through `conftest.py` on every `pytest -k`; the largest non-domain cost per run. | Not deletable; speed it up only with a measured benchmark. |
| C10 | `static/js/app.js` `VARIANT_BOOLEAN_OPTIONS` / `wikiFallbackAbility` | Variant buttons flatten *forms* and *packets* onto one index, but a form toggle and a packet toggle are different axes, so one index per option cannot serve both. Live: Gnar's Q flattens to Boomerang / Boomerang-reduced / Boulder Toss, and `mega: 1` makes the reduced Mini packet read as Mega while Boulder Toss (the real Mega Q) sends `mega: false` — W/E meanwhile render Mega. | Stamp each flattened variant with its source form, and read form toggles from the form rather than the flat index. |
| W2S1 | `champions/pyke.py` P + `stats.calculate_total_stats` | Gift of the Drowned Ones is a stat *conversion* the stat layer cannot express: Pyke's maximum health may not rise except by growth, and bonus health becomes 7.143% of itself as bonus AD instead. Probe at level 18 with Warmog's Armor: health 2540 -> 3660 (the game denies him all 1120) and attack damage unchanged at 96 (the game gives him +80). Both halves are wrong and they compound — his effective health is overstated and his damage understated on every build carrying health. | Needs a per-champion stat-conversion hook where item stats are folded, not a champion slot. |
| W2S2 | `champions/slotlib.py` `attach_self_shield` | The payload can only ride an ability that emits damage events, so a shield-only slot still has no channel: Shen P, Rakan P and Jarvan IV W remain unpriced, and Tahm Kench E's active (grey health -> a 2.5 s shield) cannot be authored at parse time at all because the pool is walk state. Vi P worked only because Q, E and R all emit. | Either a zero-damage shield channel in the participant ledger, or a walk-time grey-health-to-shield primitive beside `_grey_health_receipts`. |
| W2S3 | `champions/blitzcrank.py:199`, `camille.py:274`, `malphite.py:75`, `senna.py:132`, `sett.py:208`, `shyvana.py:145`, `skarner.py:62`, `neeko.py:110`, `akshan.py:327`, `renata_glasc.py:92`, `ambessa.py:106`, `rakan.py:65` | A module-authored `self_shield_events` payload is not gated on the primary defender, and the participant ledger de-duplicates shields only by event id, so a roster fight grants the champion one shield **per enemy** — five Mana Barriers against five enemies. Heals have `actor_wide` for exactly this; shields have no equivalent. Vi P gates on `roster_target_index == 0` to avoid it, which makes the two conventions disagree. | Give the shield payload the `actor_wide` flag heals already carry, and de-duplicate in `participant_timeline` rather than per module. |
| W2S4 | `docs/behavior-frontier.json` | The committed receipt is stale in blocks the gate does not compare: `--check` only flags *growth*, so `calculator/item_effects.py class_c` (431 committed, 430 measured, stale before this campaign) and `_SUPPORT_PACKET_CLAIMS` (18 committed, 19 measured after the wave-0 support work) sit unnoticed, and any `--write` silently absorbs them into an unrelated diff. | Compare the whole receipt, or split the growth-gated counters from the descriptive ones. |
| CF1 | `rune_effects.RuneConditionalAmpEffect` / `RuneFlatAmpEffect` vs `item_behavior_catalog.KEYSTONE_AMPS` | Two rune amp kinds keep their ratio in the dataclass while the keystone amps ride the one `AmpChainSlot` chain. The flat kind's ratio is option-chosen and `DeltaAmpRule.magnitude` has no option-keyed shape; the conditional kind fits (`LivePredicate(TARGET_HEALTH_FRACTION, LT|GT)`) but needs a `Comparison.GT` branch in `interpreters/delta_amp.py`, a new slot, two `rule_owners()`, and retires `AmpCondition`. `KEYSTONE_AMPS` states which amps are off-chain and why, and is now a misnomer. | Move the conditional kind onto the chain in one change; give `DeltaAmpRule` an option-keyed magnitude or leave the flat kind documented. |
| CF2 | `damage._effective_timed_cooldown` | A variant slot key such as `Q2` matches neither the basic-ability-haste branch nor the ultimate-haste branch, so Shojin-class haste and ultimate haste both skip it. | Resolve the variant to its base slot before branching. |
| CF3 | `champions/samira.py` P | Daredevil Impulse's melee blade-zone rider (2:21 +3.5%:11.32% AD, doubled at missing health) is cached and priceable; it needs a range-gated per-hit rider that attaches to ability parts as well as autos — an axis no module has. | An `add-champion` unit once the rider axis exists. |
| CF4 | `champions/ornn.py` P, `data/champions.json` Ornn | Every cached Ornn passive effect has `leveling: []`; Temper's 9%:17.94% Brittle consume lives only in prose, and Brittle itself needs an apply/consume timeline. | Prose-sourced per-level array plus a Brittle timeline; `add-champion`. |
| CF5 | `champions/zilean.py:44-46` | Chronoshift's `(600, 850, 1100)` and 200% AP ratio are literals although the cached R `Heal` row states them. | Read the row. |
| CF6 | `combat.participants[].stats` vs `champion_stats` | Two published stat surfaces disagree for every champion with an ability stat buff (Tristana Q, Olaf R, Lulu W/R, Warwick W, …): the participant card excludes the buff, the fight's `champion_stats` includes it. `/api/loadout-stats` now carries the rune page but not ability buffs either. | One stat surface, or label which fight state each reports. |
| CF7 | `champions/module_contract.py`, `tests/coverage_truth.py` | `out_of_scope` is never checked against `SLOTS`, so a hand-declared map can call an emitted slot out of scope (85 slots did before wave 2). Kalista W, Jayce E, Braum P and Ekko P read `modeled` while emitting nothing at default options, and no gate covers option-gated emptiness. | A contract rule `out_of_scope ⇒ slot ∉ SLOTS`, and a coverage-truth sweep over default options. |
| CF8 | `ability_spec.py:457` `cc_kind` | Crowd control is one vocabulary string per part with no duration and no magnitude, so eleven `cc_only` slots (Bard R, Kalista R, Nasus W, Rammus E, Renata Glasc R, Singed W, Trundle E, Twitch W, Udyr E, Viktor W, Zilean E) can be declared but never priced. | A duration and magnitude field on the CC marker, then the slots. |
| CF9 | `champions/slotlib.py:878` `stat_buff` dispatch | No movement-speed key: fifteen `aura_or_stat` slots (Nami P, Taliyah P, Teemo W, Cassiopeia P, Jayce P, Vayne P, Vladimir P, …) have no channel, while runes already carry `RuneStat.MOVE_SPEED_PERCENT` into published move speed. | One movement-speed stat channel shared by items, runes and abilities. |
| CF10 | champion on-hit rows (`warwick.py`, `shaco.py`, `sylas.py`, `xin_zhao.py`) | The champion on-hit channel cannot crit (Shaco P and Sylas P lose their crit modifiers; ability parts have `crit_effectiveness`, on-hit rows none), and `damage.py:6705` `count_ability_hits` is kit-wide so Xin Zhao P counts autos only (W's two stacks unpriced, disclosed). | A crit flag on on-hit rows; a per-slot stack source. |
| CF11 | `data/champions.json` | Cache quality the wave found: Milio P `Per-Level Scaling` holds 40 values for 18 levels (two arrays concatenated); Sylas P and Xin Zhao P carry no leveling rows; Miss Fortune P stores two rows under one attribute with no level axis; Dr. Mundo P, Rek'Sai P, Zac P, Tahm Kench E heal rows are labelled `Max Health Damage`; Jayce P has duplicate `Hextech Capacitor` entries; Nimbus Cloak's rune `leveling` is keyed by summoner-spell cooldown; Jack of All Trades' adaptive force parse drops 8/12/20. | Parser fixes on patch day, each pinned; until then the modules carry `HARDCODED` constants. |
| CF12 | `healing_legacy.py` Zac (`zac.py:1163-1170`) | The Goo chunk heal carries an `amount_formula`, so the one-pair receipt publishes `amount: 0.0` and top-level `self_healing` reads 0.0; only the coupled walk resolves it (203.0). Zac P's packet row also prices the chunk-*heal* percentage as target-max-health *damage* (resolves to 0.0 today). | Resolve the formula in the one-pair receipt; drop the heal-as-damage row. |
| CF13 | `healing_reduction.py:99-104`, `survival/transitions.py:1963` | `GRIEVOUS_WOUNDS_SOURCE_LABELS` cites Kled's Pocket Pistol as its reason and `kled.py:218` declares an empty set (wound removed V25.14) — the override has no users. `healing_reduction_sources` is a live-window view while `healing_reduced` / `healing_reduction_events` are cumulative, so a row can read `sources: []` beside a non-zero `healing_reduced`. | Delete the unused override; publish the sources cumulatively or label the window. |
| CF14 | `tests/js/champion_options_harness.mjs:15-49`, `tests/js/rune_page_harness.mjs:15-50` | The VM bootstrap is duplicated byte for byte. | Extract when a third harness appears. |
| CF15 | `stats.py:473` vs `:502` | Swiftmarch's adaptive-force conversion reads `total_move_speed` before rune grants resolve, while `item_state_receipts` (`damage.py:12227`, `pipeline.py:538`) read the post-rune, soft-capped speed — one item, two movement speeds (Ahri + Swiftmarch + Celerity: MS 395 -> 398.95, AP unchanged). | Resolve rune grants before the conversion. |
| CF16 | `participant_timeline.py:951` Taric W | The ally copy of Bastion is withheld on purpose: a per-recipient ratio would have to ride the packet, and `_support_effect_templates` caches templates per pair packet across optimizer candidates, so a recipient-scaled amount goes stale when a candidate build changes the recipient's max health. | Recipient-scaled packets keyed into the candidate signature. |
| CF17 | `champions/trundle.py` W, `champions/rumble.py` P | Frozen Domain's 30-90% AS is applied for the whole fight (the zone is 8 s and only while he stands in it — Camille-E precedent, disclosed); Rumble's Overheated bonus AS is deliberately unpriced because it arrives with a 4 s ability lockout the engine cannot express. | An uptime option on zone buffs; a lockout primitive for Overheat. |
