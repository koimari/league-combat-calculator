# Zero-residue ledger

One line per iteration of the zero-leftover-mechanics campaign, appended
never rewritten. Each line is the four DONE probes measured on the tree at
that moment, so a later reader can see which way each number moved and what
moved it.

The probes, in order:

1. `out_of_scope` champion slots, from every registered module's own contract
   (`get_champion_module_contract(name).coverage`).
2. Runes implemented over runes total, from `rune_catalog()`.
3. `docs/coverage-residue.json` rows, split by reason, beside whether
   `scripts/coverage_census.py check` is green.
4. Research gaps from the archetype ground-up results, closed or receipted.

| when | slots out_of_scope | runes | residue rows | census | note |
|---|---|---|---|---|---|
| 2026-09-16T20:55Z | 5 (Sylas R, Teemo P, Udyr P, Viktor P, Wukong W) | 62/62 | 10 (9 unsourced, 1 certification_shape) | stale | baseline. The goal's snapshot said 5 slots naming Sivir R, 31/62 runes and 24 residue rows with 3 certification_shape: Sivir R closed earlier, the runes are all compiled, and Aatrox Q/W and Darius R are already out of the residue, so only Yone E remains of the three. Census reads stale because the Yone fix is in the tree and not yet captured. |
| 2026-09-16T21:20Z | 5 (unchanged) | 62/62 | 9 (9 unsourced, 0 certification_shape) | green | certification_shape is at ZERO. Yone E closed with no new data, which is what that reason always meant: its stored-damage events are built by the fight engine from a rule the module declares, so they carry no parts for a reviewed kind to ride, and the builder now stamps the owning entry's own reviewed state onto each one. Fail-closed both ways, driven over the builder with one flag flipped. Coarse census pairs 40 to 36, items swept clean 169 to 173, both goldens identical because a certification marker is not a number. |
| 2026-09-16T22:05Z | 5 (unchanged) | 62/62 implemented; priced 38 to 39, refused 24 to 23 | 9 (unchanged) | green | `implemented` was already 62/62, so the real probe-2 reading is the priced-against-refused split, and the frontier doc's "31 priced" is stale by 8. Built the shared axis: `RuneStat.ARMOR` and `MAGIC_RESIST`, which the closed stat set had no members for, so a resistance could not be a rune stat at all. Conditioning prices on it. Its numbers were in prose only and its cached `effects` read `{}`, so the rune parser learned the `'''bonus''' armor` shape and the reparse filled Conditioning and Unflinching and nothing else. A correction the measurement forced: holder resistances do NOT reduce the holder's own damage taken, because effective health is health, shields and healing with no resistance term, and a pure-armor Chain Vest moves no number there. What they do buy is kit scaling, measured at Malphite +11.4, Rammus +9.6, K'Sante +6.5, Braum +0.0 over ten seconds. The rune discloses both halves and its 3% of TOTAL resists stays withheld for want of a percent-of-total channel. |
| 2026-09-16T22:40Z | 5 (unchanged) | 62/62 implemented; priced 39 to 40, refused 23 to 22 | 9 (unchanged) | green | Unflinching prices on the axis the iteration before it built, its 10 bonus armor and 10 bonus magic resistance now in the cache from the same parser rule. Its gate is a switch defaulting to off: the fight decides whether enemies hold the holder inside its survival walk, after the stat block the grant would ride is resolved, and the published participant row carries no crowd-control field to read instead. Measured at Malphite +14.2 and Rammus +12.0 over ten seconds. The rune's own 2-second lingering tail is inside the held reading rather than added to it, which it discloses. The Resolve refusal table gained the guard it lacked: it was only a parametrize source, so a rune leaving `_NO_DAMAGE` would have dropped out of the suite in silence. |
| 2026-09-16T23:15Z | 5 (unchanged) | 62/62 implemented; priced 40, refused 22 (unchanged) | 9 (unchanged) | green | No rune priced, two receipts corrected against measurement, which the loop counts as closing an item only because the blocker is now the real one. Both older reasons said the engine prices only the damage the holder deals; it does not, and the participant row publishes health_damage, effective_health, survived_window and death_time. Bone Plating waits on a channel's DIRECTION: champion_damage_flat_reduction exists and works, read off the TARGET, where Guardian's Horn drops a Garen fight from 2276.8 to 1826.8, and nothing reads the holder's own. Second Wind waits on a TRIGGER: every member of RuneHealTrigger and RuneTrigger names something the holder does. tests/test_holder_side_durability.py pins each fact so the day the axis lands these fail and name what to re-read. |
| 2026-09-16T23:55Z | 5 (unchanged) | 62/62 implemented; priced 40 to 41, refused 22 to 21 | 9 (unchanged) | green | Revitalize prices, the cheapest axis of the campaign: its own receipt had already named the gap exactly, that the power is what "every heal and shield the holder applies now reads" with no RuneStat member to land in. The consumer was always there, healing_reduction builds one factor from heal_and_shield_power_percent, so only the member was missing. Its three numbers were prose too and the parser learned the wiki-link shape, matching Revitalize and nothing else. Measured on Aatrox, who heals off his own damage: self healing 4012.80 to 4320.70 over ten seconds. Its second half, a further 10% on targets below 40% maximum health, is withheld because the recovery channel carries no per-target health gate and reading it as always-on would credit it against a full-health target. |
| 2026-09-17T00:35Z | 5 (unchanged) | 62/62 implemented; priced 41 to 42, refused 21 to 20 | 9 (unchanged) | green | CF9's channel already existed and only the gate and the parse were missing, which is the third time this campaign that a refusal turned out to be a missing member or a missing parse rather than a missing capability. Approach Velocity's old reason said "no damage row reads movement speed": true of a damage row, false of the build, because Swiftmarch converts total movement speed into adaptive force. Measured on Celerity first to prove the path, then on the rune: move speed 405.0 to 431.3, attack damage 167 to 169, total 2566.30 to 2589.20. Its 7.5% is written against the TOTAL where Celerity's 1% is written against the BONUS, so one parser rule takes both spellings into one key. The gate is a switch defaulting off, because the fight model carries no position. |
| 2026-09-17T01:00Z | 5 (unchanged) | 62/62 implemented; priced 42 to 43, refused 20 to 19 | 9 (unchanged) | green | Magical Footwear prices, the fourth refusal that was a missing member plus a missing parse rather than a missing capability: no RuneStat member carried flat movement speed (Waterwalking's own disclosure names exactly that gap), and its cached effects read {}. The parser learned the plain-number '''bonus''' movement speed shape, which fills Magical Footwear (10) and Relentless Hunter (8) and nothing else; Relentless stays refused, its speed out-of-combat only. The flat channel lands beside the percent one before resolve_move_speed, so one published speed feeds Swiftmarch. Measured with Swiftmarch: move speed 405.0 to 415.0, attack damage 167 to 168, total 2566.30 to 2577.80; without it the speed still moves 340.0 to 350.0 and the total does not, which is the control the old receipt lacked and the guard against the Thornmail misreading. The free boots on a clock stay withheld with the request's-own-to-list disclosure. Both goldens identical: no golden scenario selects the rune. |
| 2026-09-17T13:40Z | 5 (unchanged) | 62/62 implemented; priced 43 to 44, refused 19 to 18 | 9 (unchanged) | green | Manaflow Band prices, and it is the fifth refusal running that named a missing member and a missing parse rather than a missing capability: its own receipt said "no rune stat channel carries mana" and then named Muramana and Archangel's Staff as what a channel would feed, so the whole gap was the closed `RuneStat` set having no mana member and the cached effects reading {}. The parser learned the two prose shapes, a step written "'''maximum''' mana by 25" and a ceiling written "up to {{as|250 mana}}", which match Manaflow Band and no other rune: Presence of Mind restores a SHARE of maximum mana and carries no "by". The grant lands in the same pool an item's mana lands in, behind the same manaless gate, and the page's first total moved above the mana block so both channels an item converts into adaptive force are settled before the conversions read them. Measured on Ezreal at ten stacks: with Muramana max mana 2565 to 2815, attack damage 210 to 215 (2% of the 250, exactly), total 4060.80 to 4164.60; with Archangel's bonus mana moves 250 and ability power 76 to 78; with NEITHER item the pool still moves 250 and the total does not, which is the control, and on manaless Garen the pool does not move at all. Both goldens identical, so the reorder is a pure refactor numerically. The missing-mana regeneration half stays withheld: the rotation is not resource-gated. |
| 2026-09-17T14:20Z | 5 (unchanged) | 62/62 implemented; priced 44 to 45, refused 18 to 17 | 9 (unchanged) | green | Biscuit Delivery prices the half it keeps, and this one needed no new channel at all: `RuneStat.BONUS_HEALTH` was already a member and already read, so the whole gap was the parse its own receipt had named ("the cache carries the biscuit's sale price and not the health"). The parser learned two shapes, the permanent grant written "'''maximum''' health ... by {{as|30" and the delivery count taken from the clock times the rune names rather than from a number it never writes, which is how the option's ceiling of three is sourced. The second blocker it named, that the fight consumes no biscuits, was never a blocker but a question: how many have been drunk or sold by the time the fight opens is banked progress the request states, the same shape Manaflow Band's stacks take, and zero is the default. Measured at three biscuits: Garen's health 2356 to 2446, exactly the 90, and his total 2456.40 unmoved, which is the control; Cho'Gath, whose rotation reads maximum health, moves the same 90 and 3119.40 to 3128.40. The restore itself stays withheld: a biscuit is drunk between fights, on a clock the fight model has none of. Both goldens identical. |
| 2026-09-17T16:10Z | 5 (unchanged) | 62/62 implemented; priced 45 to 47, refused 17 to 15 | 9 (unchanged) | green | The two holder-side axes, built together because they turned out to be one lane. `tests/test_holder_side_durability.py` had measured both blockers and both readings were right about the fact and wrong about the conclusion. Second Wind's receipt said no rune trigger fires on damage TAKEN: still true of both vocabularies, and it never needed one, because the survival walk arms Doran's Shield's regeneration window off the incoming hit itself. Bone Plating's said the flat-reduction channel is read off the TARGET: still true of the item field, and the rune never fitted that field anyway, since an activation that arms on one hit, pays three after it and then waits out 55 seconds has no room in a single target number. Both are priced on the walk, which is the one place the packets the holder RECEIVED are held in order with their times. Four numbers came out of prose the parser had no rule for (a 4% share over 10 seconds, 3 hits over 1.5 seconds), each matching its own rune and no other. Measured on Garen against Darius over ten seconds: Second Wind takes healing received 0.0 to 19.6 and pays 79.7 to 108.0 beside Doran's Shield, so one holder may declare both halves of the lane; Bone Plating takes damage taken 731.2 to 618.4 against a level-6 Darius and moves death 3.75 to 4.103 against a level-18 one, with the holder's own total unmoved. Both goldens identical: no golden scenario selects either rune, and the item channel was left exactly where it was. Second Wind's reading is a floor, since its refresh is not replayed; Bone Plating's is a ceiling against a multi-enemy roster, since the walk does not read which champion armed it. |
| 2026-09-17T17:05Z | 5 (unchanged) | 62/62 implemented; priced 47 to 48, refused 15 to 14 | 9 (unchanged) | green | Absorb Life prices and two receipts are corrected against the tree, which is the shape iteration 5 took. Its refusal said the pair engine prices one champion against one champion and there is nothing to kill: the first half was already false, since a fight may be run against a minion class, and the second is false of any fight the target does not survive, which is the exact event Triumph is paid on. So the rune rides `RuneHealTrigger.TAKEDOWNS` at the same dated instant. Measured on a Garen who kills: self healing 0.0 to 23.0, the level-18 column of its own table, and 0.0 in the fight the target survives. Every other kill a lane pays it on stays out, so the reading is a floor. The two corrections: Demolish said the engine prices one champion against one champion, when the target vocabulary is champion AND minion and what it actually lacks is a structure class; Cosmic Insight said an item active is priced once per fight whatever its cooldown, which is true of the once-per-fight active stream and false of the empowered-auto one, where `_add_auto_cooldown_strikes` walks Titanic Crescent's declared cooldown and counts a proc per window. That stream is the channel item haste would reach, and the receipt now names it. Both goldens identical; full suite 16413 passed. |
| 2026-09-18T18:16Z | 5 (unchanged) | 62/62 implemented; priced 48 to 49, refused 14 to 13 | 9 (unchanged) | green | Cosmic Insight prices on the channel its own corrected receipt had named, the sixth refusal running that was a missing member plus a missing parse rather than a missing capability: no `RuneStat` member carried item haste, and its cached effects read `{}`. The parser learned the two section-anchored spellings, which match Cosmic Insight and no other rune (18 summoner-spell haste, 10 item haste); the summoner half is parsed for the record and compiled by nothing, the Ionian-Insight shape. The grant lands in a new published `item_haste` stat beside the other hastes, and the empowered-auto stream walks Titanic Crescent's declared cooldown through the shared haste formula, so ten haste shortens ten seconds to 9.09. Measured on Garen with Titanic Hydra and a Dagger: Crescent procs 2 to 3 in a 20-second window and 3 to 4 in a 30-second one, the row's +45.8 IS the total's +45.8 both times, so nothing else reads the channel. Two controls: on Garen's own coarse swing grid the count holds at 2 and the rune prices nothing, which is the honest boundary the disclosure carries; without Titanic the stat card still gains its 10 and the total holds still, which is the Thornmail guard. A determinism trap was caught mid-iteration: `deterministic` is a `calculate_payload` keyword, not a request key, so the first probe runs rolled crits through Phantom Dancer and the totals jittered run to run; the measurement build carries no crit and passes it as the kwarg. The new published key moves both goldens by membership only (pair 519, coupled 169, every one `item_haste` at 0.0 or MEASURED/applied, zero value moves), claimed path-by-path in `docs/receipts/expected-golden-diff-cosmic-insight-item-haste.json`; the capture follows at the phase boundary because it refuses a dirty `src/` tree. Full suite 16420 passed, 11 skipped; black/pylint (9.75 unmoved, no new E/F)/prose/swing-audit/coverage-status/casts all clean; the literal-defaults ratchet takes one row for the new compiler, and 47 test stat builders take the `item_haste` key the direct read demands. Jev classified all 14 refusals advisory-only to order the queue: Cosmic Insight and Demolish priceable (new axis), Nimbus Cloak and Triple Tonic split, the rest zero in-fight; Triple Tonic's elixirs carry empty atoms everywhere and Nimbus needs a decay shape plus duration plumbing neither source holds, so both stay refused with the blockers named. |
