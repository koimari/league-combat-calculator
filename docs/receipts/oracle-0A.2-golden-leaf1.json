{
  "receipt_id": "oracle-0A.2-golden-leaf1",
  "runbook": "R-18 / R-19 (fresh independent oracle)",
  "scenario": "metadata (pair-engine golden, scripts/golden_baseline.json, re-captured in commit 2975e7b)",
  "leaf_path": "/metadata/snapshot_kind",
  "old_value": null,
  "old_value_note": "<absent> — the leaf did not exist in the prior baseline",
  "new_value": "pair",
  "verdict": "new_value_correct",
  "leaf_class": "provenance/metadata, not a game quantity",
  "evidence_tree": {
    "built_with": "git archive --format=tar HEAD data docs/math-foundations.md | tar -x -C <scratch>",
    "scratch_dir": "C:/Users/skywa/AppData/Local/Temp/claude/C--Users-skywa-Desktop-claude-code-merged-calculator/a5d13eb4-3688-46ad-b045-6ef563ce6f18/scratchpad/oracle0A2",
    "read": ["data/ (full tree)", "docs/math-foundations.md"],
    "not_read": ["src/", "tests/", "scripts/", "docs/plans/", "docs/receipts/", "git history", "the diff", "the plan"]
  },
  "independent_computation": {
    "method": "Attempted to derive the leaf value from the two oracle sources permitted by the runbook: cached wiki text under data/ and quoted formulas in docs/math-foundations.md.",
    "steps": [
      {
        "step": 1,
        "action": "grep -rn \"snapshot_kind\" over the whole exported evidence tree (data/ + docs/math-foundations.md)",
        "result": "zero matches",
        "conclusion": "No cached wiki record and no quoted formula defines, constrains, or mentions a snapshot_kind field."
      },
      {
        "step": 2,
        "action": "grep -rn -i \"pair.engine|pair_engine\" over the evidence tree; grep -rln '\"metadata\"' over data/",
        "result": "zero matches for both",
        "conclusion": "No cached data file carries a metadata block of this shape, so there is no sibling record to cross-check the value against."
      },
      {
        "step": 3,
        "action": "grep -rn -i \"pair\" docs/math-foundations.md",
        "result": "3 hits — line 386 (`score_memo`, `pair_result_cache`, `CoupledSearchContext`), line 430 (`(model, observed)` pairs in the conformal-prediction discussion), line 468 (per-tick pairing, unrelated)",
        "conclusion": "The only engine-vocabulary hit, `pair_result_cache` in Section 3.2, confirms that 'pair' is the established in-repo term for the engine's paired (attacker/defender candidate) evaluation path. The token 'pair' is therefore a coherent, in-vocabulary descriptor for a snapshot produced by that path; it is not a foreign or contradicted label."
      },
      {
        "step": 4,
        "action": "grep for 'snapshot' in docs/math-foundations.md and data/README.md",
        "result": "one hit, docs/math-foundations.md:428 — 'The golden snapshot (`scripts/golden_snapshot.py`) and the E-series corpus pin point estimates against game-verified scenarios.'",
        "conclusion": "The document establishes that the golden snapshot is a pinning/provenance artifact over scenarios. It says nothing about how the snapshot self-describes its own kind, i.e. it neither supplies nor forbids a snapshot_kind tag."
      }
    ],
    "result": "NOT ORACLE-COMPUTABLE. /metadata/snapshot_kind is a self-description of the baseline artifact (which capture path produced it), not a derived League quantity. It has no wiki antecedent and no formula antecedent, so no numeric or textual value can be independently recomputed from the permitted sources."
  },
  "sources_quoted": [
    {
      "source": "docs/math-foundations.md:386 (Section 3.2, Approximation and its guarantees)",
      "quote": "The score memo (`score_memo`, `pair_result_cache`, `CoupledSearchContext`) reuses every evaluation a candidate swap cannot change, which is what makes thousands of coupled evaluations tractable",
      "bearing": "Establishes 'pair' as the repo's own term for the paired-evaluation engine path; makes 'pair' a consistent value for a snapshot-kind tag rather than an invented token."
    },
    {
      "source": "docs/math-foundations.md:428 (Section 4, Variance and confidence)",
      "quote": "The golden snapshot (`scripts/golden_snapshot.py`) and the E-series corpus pin point estimates against game-verified scenarios.",
      "bearing": "Defines the golden snapshot's role as pinning/provenance; consistent with the baseline carrying provenance metadata, but does not itself dictate the field or its value."
    },
    {
      "source": "data/ (full exported tree) — negative evidence",
      "quote": "grep -rn \"snapshot_kind\" -> no matches; grep -rln '\\\"metadata\\\"' data -> no matches",
      "bearing": "No cached wiki text bears on this leaf in either direction."
    }
  ],
  "verdict_rationale": [
    "The leaf is a provenance tag on the baseline artifact, not a computed game value, so the oracle sources permitted by the runbook cannot adjudicate it on game truth. This is reported rather than papered over.",
    "Given that, the old state <absent> is not a competing correct value: absence is the absence of a claim, not a claim that the snapshot has no kind. So old_value_correct is not defensible.",
    "The new value 'pair' is not contradicted by anything in the evidence tree, and it matches the only in-repo vocabulary for a paired-evaluation path (`pair_result_cache`, math-foundations Section 3.2). A baseline that names the engine path that produced it is strictly more self-describing than one that does not, which is the direction the golden-gate semantics (a baseline you must be able to explain diffs against) want.",
    "both_wrong would assert 'pair' is an incorrect descriptor; no evidence in the permitted sources supports that assertion.",
    "Confidence is therefore MODERATE and scoped: correct-as-a-non-contradicted, in-vocabulary provenance tag — NOT verified against game data, because no game data speaks to it. A reviewer who wants a hard verification must check `scripts/golden_snapshot.py`'s capture path, which this oracle is forbidden to read."
  ],
  "confidence": "moderate (non-contradiction + vocabulary consistency; no game-truth verification possible for a metadata leaf)",
  "committed": false
}
