{
 "artifact": "campaign_stages",
 "rule": "D-40: every frontier commits its exclusion lists beside its counters, diff-gated by set equality, rather than inside the tool that measures it. Counter 4's overdue rule (umbrella criterion 7, Amendment B) has exactly one trigger -- 'the stage this row defers to has shipped' -- which makes it the most load-bearing list of the three, and it lived as a literal dict inside scripts/behavior_frontier.py. It lives here now. What is NOT declared here is shippedness itself: each row names the slice tag the campaign's commit subjects carry, and the gate reads the tree for it, so a stage that ships makes its own rows overdue with nobody remembering to edit anything.",
 "how_shipped_is_derived": "A stage has shipped when the campaign range holds a commit whose subject's trailing tag list carries its slice_tag AND a commit carrying its followed_by tag. Two conjuncts, because the first alone fires on the stage's own first commit -- a stage is not shipped while it is being shipped -- and the campaign's own convention is that the next stage's tag appears only once the previous one is done. The derived fact is the earliest sha carrying each tag, which is stable: later commits do not move it, so the frontier receipt does not churn on every commit.",
 "campaign_range": "584071e..HEAD",
 "why_a_deferral_may_not_name_an_undeclared_stage": "This is the hole the literal dict left. Nothing derived stage completion from the tree, so a future stage that shipped without retiring its rows produced exactly the silence the overdue rule was written to end -- unless a human remembered to add a line to the measuring tool first. Requiring every retiring stage to resolve to a row here inverts that: the stage record must exist before the deferral can, and from then on the tree decides when it comes due.",
 "why_a_record_declares_what_it_is_the_creditor_of": "Resolving a deferral's stage against 'some record here' is weaker than it reads, and the weakness only showed once a re-dating happened: the set has two members, one of them the stale stage Amendment K moved the rows OFF, so a clause reading 'the recorded stage is one this file declares' was satisfied by exactly the state the ruling existed to end. creditor_of is the other half. Exactly one record may claim a given debt -- scripts/behavior_frontier.creditor_stage raises on zero and on two -- and every unserved-lane row is dated from that claim rather than from a literal in the measuring tool or a stage name typed beside it.",
 "why_this_file_is_the_only_home_of_a_retiring_stage": "Until 2026-08-15 the stage was ALSO declared per row on interpreters.UnservedLane.retires_at, and the gate bound the two copies together. That binding is why Amendment K's re-dating had to edit a runtime module to land -- campaign bookkeeping with no runtime consumer, sitting in src/, making a ruled re-dating a src/ commit -- which R-35 round 5 returned as the structural cause of the receipt-walk ruling group's zero_src_change failure. The second copy is gone. src/ declares what it can check at import (which interpreter produces the number, and by which lane) and nothing about when a row retires; this file declares the debt-to-stage claim, one debt per record, and behavior_frontier resolves counter_4/<lane> from it. Two copies that a clause keeps equal are strictly worse than one copy nothing can contradict, and the clause that kept them equal is replaced by one with something left to catch: the committed frontier receipt must record the stage this file claims TODAY, so re-dating here and not regenerating the receipt is red.",
 "stages": [
  {
   "stage": "Phase 4 S3 — one kernel, five views",
   "slice_tag": "S3",
   "followed_by": "S4",
   "creditor_of": "counter_4/compiled_score_walk",
   "note": "The stage counter 4's fourteen RECEIPT_WALK deferral rows named until 2026-08-15. It shipped, and Phase 4 ran on through S10 and its boundary without retiring them. Amendment F then measured why it never could: a row retires when INTERPRETERS holds its key, and projecting a ledger's quantities onto a payload leaf registers nothing. The record stays because the stage shipped and that is a fact, because it is what the closeout re-dated the rows FROM, and because it is still the creditor of counter 4's OTHER half: the sixteen COMPILED_SCORE_WALK rows, which are not deferrals, are not netted out of any target and have named this stage since they were written. That claim is transcribed here from interpreters.UnservedLane.retires_at, which held it until this file became the only home; it re-dates nothing and rules nothing.",
   "blocked_on": "docs/receipts/escalated-defects-P4-S9.json, entry no_production_path_emits_a_non_measured_disposition: the OutcomeLedger is the receipt walk's companion since 2026-08-14, but no view projects its quantities onto a leaf, so no family gained a receipt-walk interpreter and these rows cannot retire. Re-dating them is a ruling and not a lane's edit -- and the ruling landed: umbrella Amendment K, 2026-08-15, citing Amendment F's measurement."
  },
  {
   "stage": "Closeout 2026-08-15 — the declared lane's interpreter",
   "slice_tag": "campaign-close",
   "followed_by": "campaign-close-report",
   "creditor_of": "counter_4/receipt_walk",
   "note": "The stage counter 4's fourteen RECEIPT_WALK deferral rows name since umbrella Amendment K re-dated them, citing Amendment F for the measurement behind it. It says the ACT rather than a slice number, which is the correction: the retiring act is a per-family interpreter in the lane the row itself declares -- the receipt walk for the eleven the pair engine feeds, the defence resolver for the three whose via says so. It has shipped on both conjuncts and it retired none of them, so every row stays overdue. That is the point of re-dating to a stage that has already run: a re-dating that made the rows read as on schedule would be the debt getting smaller by being re-dated.",
   "blocked_on": "docs/receipts/receipt-walk-retirement-schedule.json: the rows still standing, sized as the slices Amendment F says they are, each with the qualifying population R-20's second half requires before its first src/ edit, and each carrying the settled retiring act Amendment K rules. ONE row is left of Amendment F's fourteen -- secondary_target/receipt_walk -- and it is a behaviour-changing slice no phase budgeted. Of the thirteen that have left, nine retired by the ruled act, one closed as a schedule category error under umbrella Amendment O, Ruling 1, and three closed as lane-declaration corrections under umbrella Amendment Q (2026-08-16), because INTERPRETERS already held their (family, DEFENSE_RESOLVER) key and a family whose walk-side need is served through its declared lane must not declare a receipt-walk lane at all -- each on a ground measured in both directions and re-measured on every check, rather than by an edit from inside the counter it moves (D-40)."
  }
 ]
}
