# Calculator application + development environment.
# vendor/lolstaticdata/requirements.txt belongs to the vendored external project;
# its overlapping scraper packages are intentional.

# Web framework
Flask==3.1.3
gunicorn==26.1.0  # production WSGI server (Linux/Docker only; run_web.bat uses the Flask dev server)

# Data fetching & parsing (used by lolstaticdata)
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
SLPP==1.2.3
dataclasses-json==0.6.7
natsort==8.4.0
RapidFuzz==3.14.6
thefuzz==0.22.1
stringcase==1.2.0

# Testing
pytest==9.1.1
pytest-xdist==3.8.0  # parallel test execution (CI runs `pytest -n auto`)

# Code quality
black==26.5.1
pylint==4.0.7
bandit==1.9.4
pip-audit==2.10.1

# P6 persistence: PostgreSQL via SQLAlchemy 2.x with the psycopg3 driver.
SQLAlchemy==2.0.52
psycopg==3.3.4

# P0a managed Redis result-cache backend.
redis==8.1.0

# P0b error tracking (lazy import; only initialised when SENTRY_DSN is set).
sentry-sdk==2.68.0

# P0b load sanity test (scripts/load_sanity.py).
httpx==0.28.1
