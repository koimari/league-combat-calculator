You are building the OPTIMAL-EVENT-ORDER engine (F2) for the Scryglass calculator — the 'combo' layer that derives the most optimum play per champion from the atomized ability data.

YOUR WORKTREE: /Users/river/Projects/lcc-f2 (branch codex/f2-rotation). Python: /Users/river/Projects/league-combat-calculator-audit/.venv/bin/python.

THE VISION (product owner): "combos have to be wired in — Cassiopeia would realistically use Q before Es, and aim to reapply Q as efficiently as possible; Aphelios with X weapons would have to do A B and C actions in order to maximize damage. This has become easier to grasp once we atomize effects and champions, since we can objectively derive the best order of events." The user's example: the current event order showed Cassiopeia using E before Q — unrealistic; the fight should cast the OPTIMAL rotation (apply-before-consume, reapply buffs/poisons, detonate stacks at the right moment).

CONTEXT: src/calculator/damage.py computes a rotation from a DEFAULT_CAST_ORDER (fixed Q,W,E,R). The atomized data lives in data/champions.json (per-ability leveling rows with attribute names like 'Damage', 'Total Magic Damage', '% of target's maximum health', cooldowns) + the champion modules in src/calculator/champions/ (options, parts with DamagePart metadata, dot_duration, post_hit_proc, empowers_next_auto, on_hit, cc_kind). The event order shown in the UI comes from the fight's cast_order + breakdown damage_events.

DELIVERABLES:
1. docs/rotation-design.md — the design: how to derive the optimal per-champion rotation from atomized data. Define the scoring: for each champion, rank its damaging abilities by (a) DPS contribution per rank at the fight's stats, (b) setup/consume relationships (e.g. Cassiopeia Q poisons BEFORE E spam; Varus Q detonates Blight applied by autos/W; Brand E spreads after Q/R apply Blaze; Aphelios weapon pair dictates Q; Karthus Q isolated single-target), (c) cooldown gating (cast order must respect cooldowns), (d) buffs first (Aatrox R AD amp before damage, Vladimir R before burst, Jhin R after setup). Encode these as a per-champion 'combo priority' table derived from the atomized attributes + module metadata, with a documented fallback (default Q,W,E,R order) for champions with no combo signal.
2. IMPLEMENTATION: a rotation resolver in src/calculator/ (new module rotation_resolver.py or in damage.py) that computes the fight's cast_order from the champion's atomized data + the combo table, replacing the naive DEFAULT_CAST_ORDER when a combo exists. It must respect per-rank cooldowns in time-based mode (recast when off CD, reapplying setup abilities at their optimal cadence — e.g. Cassiopeia Q at its CD then E-spam until Q comes back).
3. Per-champion combo table for the first batch (at least 10 champions with clear combos): Cassiopeia (Q→E spam), Varus (autos/blight→Q detonate), Brand (Q/R apply Blaze→E spread), Vladimir (R first→Q/E/W burst), Aatrox (R→damage), Jhin (4th shot setup), Annie (passive stun→W/R burst), Lux (E slow→Q root→R), Zed (W shadow→E→Q→R), Aphelios (weapon-pair Q priority). Each with the sourced rationale (which atom/attribute drives it).
4. The /api/calculate response gains a 'rotation' receipt: {order: [Q,E,E,E,...], rationale: "Q poisons, E consumes"} so the UI can show WHY the order is optimal.
5. A NEW self-contained static/js/eventorder.js module rendering the event order WITH the combo rationale (do NOT edit static/js/app.js — another agent owns it; add the script tag + mount point to templates/index.html additively).
6. tests/test_f2_rotation.py — per combo champion: parse-level rotation assertions (order + rationale) + a time-based fight asserting the rotation respects cooldowns (Cassiopeia Q cast at CD intervals with E in between; Varus autos before Q detonation).

GATES: pytest -q full; pylint src/ --fail-under=9; black --check src/ tests/; node --check static/js/eventorder.js; git diff --check; golden — re-capture + explain every diff (cast orders change fight totals for the combo champions — THIS IS EXPECTED AND DESIRED).
COMMIT "feat(F2): optimal event-order engine from atomized ability data (combo layer)" and PUSH origin/codex/f2-rotation. Do NOT merge.
Reply to parent: the rotation design, the combo table (10 champs), how cast_order is derived, the /api/calculate rotation receipt, golden diff summary, tests, gates, commit SHA.